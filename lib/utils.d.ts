export declare function windowToLogical(wx: number, wy: number, w: number, h: number): {
    x: number;
    y: number;
};
export declare function logicalToWindow(lx: number, ly: number, w: number, h: number): {
    x: number;
    y: number;
};
